// Stub — Turso client implemented in Story 1.4

export async function getEpisodes(): Promise<never[]> {
  return [];
}

export async function getEpisodeBySlug(
  _slug: string
): Promise<null> {
  return null;
}
